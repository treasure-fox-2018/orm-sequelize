class View {

    static displayError(errMessage) {
        console.log(`Error message: ${errMessage}`)
    }

    static displayMessage(message) {
        console.log(message)
    }
}

module.exports = View;